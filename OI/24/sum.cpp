#include <iostream> 

using namespace std;

typedef unsigned long long ULL;

int main(){
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    int s, m, q;
    ULL dp[200][200][200];
    for(int mod = 1; mod <= m; ++mod){
        for(int suma = 1; suma < 20; ++suma){
            for(int len = 1; len < 20; ++len){ 
                if()
            }
        }
    }
}