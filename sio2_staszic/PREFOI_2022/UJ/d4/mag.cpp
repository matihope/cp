#include <iostream>
#include <vector>
#include <stack>

using namespace std;

typedef long long LL;
typedef vector<int> VI;

int main(){
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    
    int z;
    cin >> z;
    while(z--){
        int n;
        cin >> n;
        vector<int> tab(n);
        for(int i = 0; i < n; ++i)
            cin >> tab[i];

        
    }
}