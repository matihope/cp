#include <bits/stdc++.h> 

using namespace std;

vector<int> lcp;

void kmp(string& s, vector<int>& pref){
    pref[0] = -1;
    int l = 0;
    for(int i = 0; i < s.length(); i++){
        while()
    }
}

void check_fill(string& s, size_t len){
    s = s.substr(0, len) + '$' + s;
}

int main(){
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    string s1;
    cin >> s1;
    for(size_t i = 0; i < s1.size() / 2; ++i){

    }
}